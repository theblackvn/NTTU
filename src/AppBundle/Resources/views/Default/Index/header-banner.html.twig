<section id="nuti-slide" class="slider">
    <div id="slider-wrapper" class="owl-carousel owl-theme">
        {% for item in headerBanners %}
        <div class="item">
            <img src="{{ asset(item.fullSizeThumbnail) }}">
        </div>
        {% endfor %}
    </div>
</section>
<!-- //slider -->